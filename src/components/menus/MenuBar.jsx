import React from 'react';

const MenuBar = () => {
    return(
        <div className="menu-bar">
            {
                <link className="btn menu-btn">
                </link>
            }
        </div>
    );
};

export default MenuBar;